<%= form_with(model: [@test, @question], local: true) do |form| %>
  <div class="field form-group">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
