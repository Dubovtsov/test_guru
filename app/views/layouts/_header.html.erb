<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="<%= tests_path %>">TestGuru</a> 
  <% if logged_in? %>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <span>Welcome, <b><%= current_user.email %></b> Guru</span> <a href="<%= logout_path %>">Logout</a>
        </li>
      </ul>
    </div>

  <% else %>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <a class="nav-link" href="<%= signup_path %>">Signup</a>
        </li>
        <li class="nav-item active">
          <a class="nav-link" href="<%= login_path %>">Login</a>
        </li>
      </ul>
    </div>
  <% end %>
</nav>
