<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="<%= tests_path %>">TestGuru</a>
  <% if user_signed_in? %>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <span><%= t('.welcome_user_html', email: current_user.email) %></span> <%= link_to "#{t('.logout')}", destroy_user_session_path, method: :delete %>
        </li>
      </ul>
    </div>

  <% else %>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <a class="nav-link" href="<%= new_user_registration_path %>">Signup</a>
        </li>
        <li class="nav-item active">
          <a class="nav-link" href="<%= user_session_path %>">Login</a>
        </li>
      </ul>
    </div>
  <% end %>
</nav>
