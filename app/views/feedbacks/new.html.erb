<div class="row justify-content-center mt-3">
  <div class="col col-md-4">
    <h2>Форма обратной связи</h2>

    <div class="card border-iron bg-light-grey">
      <div class="card-body">
        <%= form_with(url: feedbacks_url, method: "post") do |f| %>
          <div class="form-group">
            <p>
              <%= f.label "Текст сообщения" %><br />
              <%= f.text_area :body, autofocus: true, class: "form-control" %>
            </p>

            <p>
              <%= f.label 'От:' %><br />
              <%= f.text_field :email, value: current_user.email, class: "form-control", disabled: true %>
            </p>

            <%= f.submit t('.send'), class: "btn btn-primary" %>

          <% end %>
          </div>

      </div>
    </div>
  </div>
</div>
